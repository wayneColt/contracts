# manifest.yaml — declared module. No runner has measured it yet; the contract files land when one has.
module: recon-billed-vs-received
status: declared
job: "does what we were billed match what we got"
in: "two document sets"
out: "matched lines, exceptions, dollar delta"
clock: "paused"
substrate: "cpu-8"
packet:
  conserved: "line items sum to the total"
  controllable: "both document sets"
  hidden: "mis-keyed part numbers"
  noise: "OCR error"
rung_0: "no standard, no run — the buyer writes the rubric before the module exists for them"
